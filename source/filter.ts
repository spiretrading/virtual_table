/** Specifies the filter of a column. */
export enum Filter {

  /** No filter can be applied to the column. */
  UNFILTERABLE,

  /** The column has no filter. */
  NONE,

  /** The column has a filter. */
  ANY
}
