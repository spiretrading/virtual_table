/** Specifies the sort order. */
export enum SortOrder {

  /** No sorting. */
  NONE,

  /** Sorted low to high. */
  ASCENDING,

  /** Sorted high to low. */
  DESCENDING,

  /** No sorting. */
  UNSORTABLE
}
